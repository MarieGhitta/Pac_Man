"""Represent the ghost types."""


from enum import Enum, auto


class GhostType(Enum):
    """Represent the ghost types."""

    BLINKY = auto()
    PINKY = auto()
    INKY = auto()
    CLYDE = auto()